@page "/image-catalog"

<PageTitle>Галерея</PageTitle>

@foreach (var imageFile in ImageFiles)
{
    <img src="@imageFile.FilePath" alt="@imageFile.Name" style="max-width: 200px; max-height: 200px; margin: 10px;" />
}